    <div class="toggle-container" style="display: flex;justify-content: center;">
        <button id="APPLY">
            <img src="/icons/Moon.svg" id="moonImage" class="moon">
            <img src="/icons/Sun.svg" id="sunImage" class="sun">
        </button>
    </div>

    <style>
      #toggle-container {
          text-align: center;
          position: absolute;
          display: flex;
          justify-content: center;
      }
      #APPLY {
          background: #00000008;
          width: 40px;
          height: 40px;
          border: none;
          cursor: pointer;
          
      }
      #APPLY img {
          width: 30px;
          transition: transform 0.3s ease;
      }
      #APPLY img.moon {
          display: none;
      }
      #APPLY img.sun {
          display: block;
      }
      #APPLY.active img.moon {
          display: block;
      }
      #APPLY.active img.sun {
          display: none;
      }
      #astrothing {
          /* Just in case */
      }
  </style>

    <script lang="js">
        let isDarkMode = false;

        function toggleDarkMode() {
            const button = document.getElementById('APPLY');
            const moonImage = document.getElementById('moonImage');
            const sunImage = document.getElementById('sunImage');
            const Sode = document.querySelector(".container")
            const body = document.body;

            isDarkMode = !isDarkMode;

            button.classList.toggle('active');
            moonImage.classList.toggle('active');
            sunImage.classList.toggle('active');

            body.classList.toggle('dark-mode');

            //const backgroundColor = isDarkMode ? '#333' : '#f0f0f0';
            //body.style.backgroundColor = backgroundColor;

            const textColor = isDarkMode ? '#f0f0f0' : '#333';
            body.style.color = textColor;

            moonImage.style.display = isDarkMode ? 'block' : 'none';
            sunImage.style.display = isDarkMode ? 'none' : 'block';

            Sode.style.display = 'Block'
        }

        document.addEventListener('DOMContentLoaded', function() {
            const storedState = localStorage.getItem('colorState');
            if (storedState === 'dark') {
                toggleDarkMode();
            }
        });

        document.getElementById('APPLY').addEventListener('click', toggleDarkMode);

        // there is Two just in case my code messes up
        document.getElementById('APPLY').addEventListener('click', toggleColors);

    </script>
</body>
