<header id="Main-Header">
  <a id="Main-Title" href="/">
      <img src="/NJROTC-Seal.svg">
      Parlier NJROTC
  </a>
  <label for="box" id="Main-Menu">&#8801; <!--This is the ≡ (Hamburger) character, DO NOT DELETE --></label>
</header>

<style>
  /* Universal */
  #Main-Header{
      position: relative;
      left: 0;
      top:0;
      max-width: 100vw;
      height: 12vh;

      padding-inline: 5vmin;
      filter: none; 

      display: grid;
      
      align-items: center;
      justify-items: stretch;

      grid-auto-flow: column;
      grid-template-columns: 1fr 1em/* em = font size */;

      border-block: 1px solid lemonchiffon;
      background: linear-gradient(15deg, rgb(0, 0, 80),navy,#6F4E37, gold);

      -webkit-user-select: none; 
      -moz-user-select: none; 
      -ms-user-select: none; 
      user-select: none; 
  }
  #Main-Header a{
      color:white;
      text-decoration: none;

      font-size: 3.2vh;

      display: flex;
      align-items: center;
      justify-content: center;

      gap:1.5vmin;

      justify-self: left;

      font-family: "Josefin Sans" ,'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
      font-weight: bolder;
  }
  #Main-Header label{
      justify-self: right;
      font-size:5vh;
  }
  #Main-Header img{
      height:5vh;
  }

  /* Desktop Layout */
  /* Moblie Layout */

</style>

<!--What is this???? - Jose -->
<div id="Importment">
<span>Test 
  <a>Link</a>
  <div style="position: fixed; right:20px;top:10px; color:black;">X</div>
</span>
</div>
<!-- very nice  -->

<!-- This style only applies here and no where else, still you can only use 1 id per item-->
<style>
    #Importment span{
      width: 99.25%;
      height: 5vw;
      top: 0px;

      position: fixed;
      display: none;

      background-color: whitesmoke;

      justify-content: center;
      justify-items: center;
      align-items: center;

      color: black;
      font-size: 30px;
      gap: 8px;

      border: double black 5px;

    }
    #Importment span a{
      animation: Span-a-Color 2s infinite;
      font-size: 30px;
      gap: 8px;

      text-decoration: ;
    }
    @keyframes Span-a-Color{
      25%{
        color:dodgerblue;
      }
      50%{
        color:rgb(87, 160, 233);
      }
      75%{
        color:rgb(156, 199, 242);
      }
      100%{
        color:white;
      }
    }
    button {
    color: #ffffff;
    background: rgb(0, 0, 0);
    width: 10vw;
    padding: 10px;
    text-align: center;
    display: inline-block;
    font-size: 18px;
    margin: 10px;
    cursor: pointer;
    border: solid 1px rgb(32, 31, 31);
    border-radius: 10px;
    align-self: right; /* This line moves the button to the right */
    transition: ease-in-out 3s;
}

    button:hover {
    background: none;
    font-size: 18px;
  }

  img {
    user-select: none;
    -moz-user-select: none;
    -webkit-user-select: none;
    -ms-user-select: none;
    -webkit-user-drag: none;
    -moz-user-drag: none;
    -ms-user-drag: none;
}

</style>
