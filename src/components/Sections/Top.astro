---
interface Props {
    image?:string;
    altTxt?:string;
    desc?:string;
    backgroundColor?:string;
    align?:string;
    /*imagePos?:string; Read Line56*/
    
}

const { image, altTxt , desc , backgroundColor = "white", align = "center", /*imagePos = "50% 50%" Read Line56*/} = Astro.props


---

<div>
    <img src={image} alt={altTxt} title={desc} decoding="async" loading="lazy">
    <span>
        <slot />
    </span>
</div>

<style define:vars={{align,backgroundColor,/*imagePos Read Line56*/}}>
    div{
        display: flex; 
        box-sizing: border-box;
        justify-content: var(--align);
        align-items: center;
        position: relative;
        padding-inline: 5vw;
        height: 600px;
        /*background-color: var(--backgroundColor); Will this Break Something? */
        border-block: 1px solid black;

        user-select: none;
        -moz-user-select: none;
        -webkit-user-select: none;
        -ms-user-select: none;
    }
    div *{
        text-align:  center;
    }
    div > span{
        z-index: 1;
    }
    img{
        position: fixed;
        top: 0px;
        left: 0px;
        
        width: 100%;
        height: 100%;
        image-rendering: optimizeSpeed;
        object-fit: cover;
        /*object-position: var(--imagePos); Tempily disabling this Cuz I Think it Useless -Saul */

        z-index: -1;
    }

    img {
        user-select: none;
        -moz-user-select: none;
        -webkit-user-select: none;
        -ms-user-select: none;
        -webkit-user-drag: none;
        -moz-user-drag: none;
        -ms-user-drag: none;
    }

</style>

