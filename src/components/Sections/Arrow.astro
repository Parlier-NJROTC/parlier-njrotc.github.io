<p class="arrow-container">
   <span class="arrow"></span>
 </p>
 <div class="line34"></div>
 
 <style>
   .arrow-container {
     display: flex;
     align-items: center;
     transition: height 9s cubic-bezier(0.85, 0.45, 0.64, 1.59);
   }
 
   .arrow {
     display: inline-block;
     width: 15px;
     height: 15px;
     border-top: 5px solid #000;
     border-right: 5px solid #000;
     transform: rotate(135deg);
     animation-name: smoothSlide;
     animation-duration: 9s;
     animation-iteration-count: infinite;
     animation-timing-function: cubic-bezier(0.85, 0.45, 0.64, 1.59);
   }
 
   .line34 {
     position: absolute;
     margin-bottom: 5px;
     width: 5px;
     height: 30px;
     background-color: #000;
     animation-name: Grow;
     animation-duration: 9s;
     animation-iteration-count: infinite;
     animation-timing-function: cubic-bezier(0.85, 0.45, 0.64, 1.59);
   }
 
   @keyframes Grow {
     0%, 100% {
       height: 26px;
     }
     25%, 75% {
       height: 42px;
     }
     50% {
       height: 61px;
     }
   }
 
   @keyframes smoothSlide {
     0%, 100% {
       transform: translateY(0) rotate(135deg);
     }
     25%, 75% {
       transform: translateY(50%) rotate(135deg);
     }
     50% {
       transform: translateY(100%) rotate(135deg);

     }
   }
 </style>
 